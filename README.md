# web-components

1. Table

> Simple table component which displays simple data from https://jsonplaceholder.typicode.com/, original source of data wasnt json, but was simple 2d array:

```sh
const newData = [
	['8831', 'foamea', 'Sibiu'],
	['6605', 'red.square', 'United States'],
]
```

Useful when receiving 2d data (rows + columns), csv
